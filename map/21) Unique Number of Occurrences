from collections import Counter
class Solution:
    def uniqueOccurrences(self, arr: List[int]) -> bool:
        mapped = Counter(arr)
        return len(list(mapped.values())) == len(set(mapped.values()))
